{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Stripe": {
    "//": "Development only. Lets the Plans page grant Pro/Premium without taking payment so the tiers can be demoed. Ignored the moment a real Stripe:SecretKey is configured, and this file is never loaded outside Development.",
    "AllowSimulatedCheckout": true
  }
}
